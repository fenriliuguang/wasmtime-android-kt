# `0.1.0` 宣称表（不是 CTS）

[English](claim-010.md) | **中文**

与英文冲突时以英文为准。坐标 **`0.1.0`**（未按下发布）。默认 NativeGpu；多数 pin 方法在 BIND 前仍是表占位。剩余自动：Dawn C 全绑定、surface 尺寸/resize、pointer/key。非紧急：`unconfigure`、带时间戳的 frame-event、Lost/Outdated `result`、多窗口。
