# `0.1.0` 宣称表（不是 CTS）

[English](claim-010.md) | **中文**

与英文冲突时以英文为准。0.1.x 产品子集；当前 GAV **`0.1.3-SNAPSHOT`** 把 `--prebuilt` `libwebgpu_dawn.so` 打进 Maven `host-dawn`。默认 NativeGpu；`.so` 在时 pin 方法走 Dawn C（含 compilation-info / pop-error-scope / uncaptured·lost / label / immediates / submit fence）。缺 `.so` 仍 Table。Remaining Table [`nativegpu-remaining.md`](nativegpu-remaining.md) 现已空。WASI leftover 已空。gfx 非紧急：`unconfigure`、带时间戳的 frame-event、Lost/Outdated `result`、多窗口。设备：Vivo V2458A / 2026-09-02，全量 instruments 绿，仓外 cube >3 min、vsync ~8.33 ms。
