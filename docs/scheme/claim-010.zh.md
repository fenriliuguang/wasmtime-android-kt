# `0.1.0` 宣称表（不是 CTS）

[English](claim-010.md) | **中文**

与英文冲突时以英文为准。坐标 **`0.1.0`**（未按下发布）。默认 NativeGpu；`.so` 在时剩余 pin 方法走 Dawn C。剩余自动：surface 尺寸/resize、pointer/key。非紧急：`unconfigure`、带时间戳的 frame-event、Lost/Outdated `result`、多窗口。
