# 非目标

[English](non-goals.md) | **中文**

与英文冲突时以英文为准。不宣称 CTS / 完整 WASI 0.3。不重写 Dawn。`0.1.2-SNAPSHOT` 发包打进 `--prebuilt` `libwebgpu_dawn.so`；后续发包仍走 Environment `release`，缺 secrets / arm64 wasmtime 或 Dawn C `.so` 不发。多窗口为非紧急。
