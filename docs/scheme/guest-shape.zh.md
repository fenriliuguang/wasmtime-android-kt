# Guest 形状（`wasi:webgpu`）

[English](guest-shape.md) | **中文**

与英文冲突时以英文为准。钉 `wasi:webgpu@0.3.0-rc.2`。方法名 / `own`/`borrow` / `async` 对齐 WIT。未接线 → `request-adapter` **`none`**。`present` 在 `wasi-gfx`，不在 webgpu。
