# Native 产物布局

[English](artifacts.md) | **中文**

正式目录：`android/jniLibs/<abi>/libwasmtime_android_kt.so`（gitignore）。`System.loadLibrary("wasmtime_android_kt")`。双 ABI：`arm64-v8a` + `x86_64`。桌面 `desktop/jniLibs/` 非正式。
