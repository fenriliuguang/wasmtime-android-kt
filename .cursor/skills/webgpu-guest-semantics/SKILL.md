---
name: webgpu-guest-semantics
description: >-
  CLOSED after F1–F9, handle-0, and full required-features. Redirects to Dawn
  consume + WG-6 leftovers. Use only if the user explicitly names the closed
  leftover-descriptor playbook; otherwise follow docs/agent/webgpu-guest-dawn.md.
---

# WebGPU leftover descriptor semantics (closed)

This queue is **closed**. Read [`docs/agent/webgpu-guest-dawn.md`](docs/agent/webgpu-guest-dawn.md) and run `.\scripts\webgpu-guest-dawn-remaining.ps1` (or `python3 ./scripts/webgpu-guest-dawn-remaining.py`).

Historical lanes: [`docs/agent/webgpu-guest-semantics.md`](docs/agent/webgpu-guest-semantics.md). Do not re-cut F1–F9 or named handle-0 / required-features.
