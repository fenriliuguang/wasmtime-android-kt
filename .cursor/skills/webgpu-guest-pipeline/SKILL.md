---
name: webgpu-guest-pipeline
description: >-
  CLOSED after P1–P5 and named first-cuts. Redirects to leftover descriptor
  semantics (blend/MRT/view-formats/mapped/hints/xr/default-queue/Dawn consume).
  Use only if the user explicitly names the closed guest-pipeline playbook;
  otherwise follow docs/agent/webgpu-guest-semantics.md.
---

# WebGPU guest pipeline (closed)

This queue is **closed**. Read [`docs/agent/webgpu-guest-semantics.md`](docs/agent/webgpu-guest-semantics.md) and run `.\scripts\webgpu-guest-semantics-remaining.ps1` (or `python3 ./scripts/webgpu-guest-semantics-remaining.py`).

Historical lanes: [`docs/agent/webgpu-guest-pipeline.md`](docs/agent/webgpu-guest-pipeline.md). Do not re-cut P1–P5 or first-cuts.
