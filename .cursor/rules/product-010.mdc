---
description: CLOSED 0.1.0 product subset — redirect 下一刀 to native-dawn
alwaysApply: true
---

# `0.1.0` product gates (empty)

`0.1.0` / P010 leftover named as that playbook only. For 下一刀 / 抖动 / hitch / 真机, stay on `fix/300-gfx-cube-pop`, read `docs/agent/gfx-hitch.md`, and run `python3 ./scripts/gfx-hitch-remaining.py` (one commit; forget inherited Closed/Likely). Native-dawn consume leftover is empty.

P0 `wasi:webgpu` **shape** is **closed**. P1 WASI 0.3 official-shape is **closed**. Do not re-cut W1–W8, P1-FS1–FS4, P1-SK1–SK2, P1-HT1, G-dev, or wasi:webgpu G1–G9 / F1–F9 / guest-pipeline / WG-6.

- `0.1.0` remaining is empty: `python3 ./scripts/product-010-remaining.py` prints a pointer to native-dawn, which prints a pointer to gfx-hitch.
- Do not invent extra `0.1.0` gates. Do not add `wasmtime-wasi` without a size + Android thread note.
- Never file GitHub issues (or Discussions-as-issues) on Wasmtime, WASI, or any other upstream. No `gh issue create`.
