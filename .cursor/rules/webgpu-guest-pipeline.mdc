---
description: CLOSED wasi:webgpu guest-pipeline P1–P5 — redirect to leftover semantics
alwaysApply: false
---

# WebGPU guest pipeline (closed)

P1–P5 and named first-cuts are **closed**. Do not start another bind-group / vertex-layout / depth / texture-mip or labels/limits/sampler/canvas-first-cut PR.

Current queue: read `docs/agent/webgpu-guest-semantics.md`. Next: `.\scripts\webgpu-guest-semantics-remaining.ps1` or `python3 ./scripts/webgpu-guest-semantics-remaining.py`.
