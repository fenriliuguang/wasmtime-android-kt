---
description: Remaining close-out (Dawn C bind, wasi-gfx size/resize)
alwaysApply: true
---

# Remaining close-out

Living auto queue: Dawn C full bind, then wasi-gfx surface size/resize, then remaining pin input streams. Read `docs/agent/remaining.md` and run `python3 ./scripts/remaining.py` (one printed Next: only).

- Needles live in `docs/scheme/remaining.md`. One lane per commit.
- BIND: remaining pin methods that Dawn C has a slot call `webgpu.h` when `libwebgpu_dawn.so` is loaded. Cube is evidence only.
- GFX-SIZE: `height` / `width` / `request-set-size` / `on-resize`.
- GFX-PIN: `on-pointer-*` / `on-key-*`.
- Named-only (never auto): `context.unconfigure`, timestamped `frame-event`, Lost/Outdated `result`, multi-window.
- Product coordinate stays `0.1.0` until that release is pressed.
- P2 Wasmtime pin is named-only. Do not re-cut closed P0 / P1 / 0.1.0 knives.
- Reuse `cm.rs` lowering. Never file upstream GitHub issues. No `gh issue create`.
