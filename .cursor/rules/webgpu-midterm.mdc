---
description: wasi:webgpu midterm lanes — canvas / S1–S3 / records / cite; no full-file reads
alwaysApply: true
---

# WebGPU midterm lanes

For 下一阶段 / WG-6 / canvas / S1–S3 descriptor / `record-*` / 真机 / WG-5, read `docs/agent/webgpu-midterm.md` and follow it.
If default shape remaining is 0 (canvas omitted) and default L2 host-fixed remaining is 0, use this instead of another label/limits L2 cut.

- Next PR: `.\scripts\webgpu-midterm-remaining.ps1` or `python3 ./scripts/webgpu-midterm-remaining.py` (one printed Next: only).
- Do not WebFetch wasi-webgpu. Use `third_party/wasi-webgpu/v0.3.0-rc.2/wit/webgpu.wit`.
- Do not Read `native/src/cm.rs` or `native/src/jvm.rs` without offset. Grep, then ~80 lines.
- One lane per PR. No product `surface-*`. No wasi-gfx. Hub freeze: no root README / `CHANGELOG.md` / `ci.yml` / `CONTRIBUTING.md`.
