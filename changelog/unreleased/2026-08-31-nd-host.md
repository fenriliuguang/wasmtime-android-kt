### Code — ND-HOST NativeGpu trait + handle table (2026-08-31)

- Rust [`NativeGpu`](../../native/src/native_gpu.rs) trait + `HandleTable` / `GpuHandle` / `ResourceKind` matching Kotlin `DawnWasiWebGpuHost` / `Handles.kt`. Handle `0` reserved. Table insert/drop does not JNI (`native_gpu.rs` has no `jni` import).
- `HostState.native_gpu` is `Option<NativeGpuHost>` (still default **None** → JNI). No product Kotlin GPU API. Consume methods still land in ND-BOOT+.
