# Unreleased 碎片

[English](README.md) | **中文**

功能 / 文档 / 修复 PR **只在本目录新增一个 markdown 文件**，**不要**改根目录 [`CHANGELOG.md`](../../CHANGELOG.md)。

文件名：`YYYY-MM-DD-<slug>.md`。标题前缀：`Code` / `Fix` / `Docs` / `Chore` / `BREAKING`。维护者用 `.\scripts\roll-changelog.ps1` 滚入主干。
