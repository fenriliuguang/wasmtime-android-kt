### Code — ND-RES native create-buffer/texture/sampler/shader/view (2026-08-31)

- NativeGpu table-backed `create-buffer`, `create-texture`, `create-sampler`, `create-shader-module`, `gpu-texture.create-view`. Shader `compilation-hints` stay **Record** (Dawn C has no slot). JNI default unchanged.
- Reuse matching `fixtures/w1` method wasm on NativeGpu via product `define_host`. Leftover table: [`docs/mapping/gap-webgpu-native-dawn.md`](../../docs/mapping/gap-webgpu-native-dawn.md).
