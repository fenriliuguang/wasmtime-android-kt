### Docs — press `0.1.0` and document consume (2026-09-03)

- Mark coordinate **`0.1.0`** as pressed (CI + device/examples gates). GAV stays `0.1.0` (`gradle.properties`, `native/Cargo.toml`); no `0.2.0`.
- Rewrite root README as the consume entry: Maven GAV, Host SPI (`Engine`/`Store`/`Linker`/`Component`/`Instance`, `GpuBackends.dawn()`, gfx vsync), guest pin, then source-build.
- Align living scheme / CONTRIBUTING / GPU-host docs: drop “not pressed”; NG-6 is Environment `release` only (not “no Central before 0.1.0”).
