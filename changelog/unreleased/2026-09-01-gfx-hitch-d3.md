### Named — cube hitch D3 Dawn C timestats / skip-present probe (2026-09-01)

- SurfaceFlinger `--timestats` on NativeGpu: enable with `-clear -enable` then `-dump` (bare `--timestats` is empty). Timeline stuffing class is n/a (`totalTimelineFrames=0` on SurfaceView); `desired2present` bimodal ~one extra vsync is the stuffing signal. Skip-present `n=6` drained that extra vsync (FPS ~104; 28–30 ms cluster gone). Gate stays off by default (`debug.wasmtime.gfx.skip_present_n`). Not a consume lane.
