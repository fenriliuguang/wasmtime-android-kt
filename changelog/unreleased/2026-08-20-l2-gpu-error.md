### Code — L2 gpu-error kind message guest fields to host (2026-08-20)

- Deepen `[method]gpu-error.kind` / `message` from omit-lane lift-only stubs to described JNI with guest device handle
- `GpuError` stores owning device rep; `get-gpu-error` still pushes `device: 0`
- New host APIs `gpuErrorKind` / `gpuErrorMessage` (Cpu stub validation-error + `cpu-gpu-error`)
