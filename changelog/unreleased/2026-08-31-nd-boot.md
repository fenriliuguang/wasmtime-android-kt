### Code — ND-BOOT native request-adapter / request-device / queue (2026-08-31)

- NativeGpu table-backed `gpu.request-adapter`, `gpu-adapter.request-device`, `gpu-device.queue`, plus adapter info / features / limits. WIT `async` stays `func_wrap_concurrent` + yield. Dawn C slots remain 0 (Cloud has no NDK). JNI default unchanged.
- Reuse `fixtures/w1` request-adapter / request-device / device-queue / adapter-info / features / limits on NativeGpu via product `define_host`. Living leftover table: [`docs/mapping/gap-webgpu-native-dawn.md`](../../docs/mapping/gap-webgpu-native-dawn.md).
