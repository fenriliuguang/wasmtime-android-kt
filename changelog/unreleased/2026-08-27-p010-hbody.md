### Code — P010 HTTP body stream<u8> (2026-08-27)

- Product `wasi:http/types` `[static]request.consume-body` / `[static]response.consume-body` return `tuple<stream<u8>, future<result<_, error-code>>>`; `[static]response.new(contents: stream<u8>)` collects a guest body. Subset: no headers / trailers / `res` future param. Constructor request body is `HBOD`. Still in-process (not wire). No `wasmtime-wasi`
- Fixture `http_body`; native `wasi_http_handler` body test; device `WasiHttpHandlerInstrumentedTest.bodyStreamEchoReturnsByteCount`
