### Gfx — remaining wasi-gfx pin streams (2026-09-02)

- Product linker registers `wasi-gfx:surface` `on-pointer-up` / `on-pointer-down` / `on-pointer-move` / `on-key-up` / `on-key-down`. Streams are empty until input is wired (missing imports are not).
