- L-SOCK-UDP: `wasi:sockets/udp-create-socket@0.3.0#create-udp-socket` + `wasi:sockets/udp@0.3.0` `[method]udp-socket.bind` / `send` / `receive`。默认只 `127.0.0.1`；非 loopback bind/send → `access-denied`。IPv6 create → `not-supported`。send/receive 在 helper 线程，**不得**在 ART 主线程做 UDP。Android 需要 INTERNET。夹具 `sockets_udp`。
