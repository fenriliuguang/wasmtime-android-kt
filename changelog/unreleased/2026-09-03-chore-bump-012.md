### Chore — bump GAV to `0.1.2` (2026-09-03)

- Coordinate **`0.1.2`** (non-SNAPSHOT). Same press pin as the `0.1.2-SNAPSHOT` cut: `--prebuilt` `libwebgpu_dawn.so` plus wasmtime opt-level 2. Consumers use `mavenCentral()`; they do not need the Central Portal snapshots repo.
