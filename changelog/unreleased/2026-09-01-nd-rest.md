### Code — ND-REST native remaining pin `[method]` suite (2026-09-01)

- NativeGpu table-backed sweep of remaining pin `[method]`s covered by `wasi_webgpu_method` (labels, supported-limits getters, WGSL features, `record-option-gpu-size64`, map-async / mapped-range, error scopes / lost, compilation-info, render bundles, canvas get-configuration stub). Dawn C slots remain 0. WIT `async` stays `func_wrap_concurrent` + yield. JNI default unchanged. Canvas `ANativeWindow` configure / present is ND-SURF.
- Reuse the full `fixtures/w1/webgpu_method_*.wasm` suite on NativeGpu via product `define_host`. Leftover table: [`docs/mapping/gap-webgpu-native-dawn.md`](../../docs/mapping/gap-webgpu-native-dawn.md).
