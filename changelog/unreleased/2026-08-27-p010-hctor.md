### Code — P010 drop HTTP constructors from the product linker (2026-08-27)

- Product `Linker.create` no longer exports `[constructor]request` / `[constructor]response`. Host supplies `request` when calling `handle`. `[static]response.new` stays. Test linker (`Linker.createWithFixtureConstructors`) keeps the constructors for leftover guests
- Fixture `http_handle` (no ctor imports). Native `wasi_http_handler` product-shaped tests (ctor guest fails to instantiate; host-pushed request + handle → 200). Device `WasiHttpHandlerInstrumentedTest.productIncomingHandlerRunReturns200` on `Linker.create`. Cloud has no device
