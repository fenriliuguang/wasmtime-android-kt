- L-ERR-HTTP: `wasi:http` `error-code` 对齐 WASI 0.3.0 官方 variant（含 payload records，末案 `internal-error(option<string>)`，去掉 `unknown`）。空 authority / 含 `/` → `HTTP-request-URI-invalid`；`https:` 且无 TLS crate → `TLS-protocol-error`；send IO 映射 `connection-refused` 等。夹具 `http_empty_authority` / `http_https_tls`。
