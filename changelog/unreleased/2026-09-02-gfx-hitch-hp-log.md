### Named — cube hitch HP-LOG device hotpath window (issue 300)

- V2458A 120 Hz Settings lock, `fullscreen-surface` NativeGpu, 150 s streamed `GfxHitch` `hotpath` + `hotpath-spike`. Mapping §6.6. Eye pop not confirmed this window. Next is HP-BIND (one sentence), not a present-path change.
