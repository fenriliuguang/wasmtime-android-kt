### Docs — wasi:webgpu midterm playbook (canvas / S1–S3 / records / cite) (2026-08-20)

- Add `docs/agent/webgpu-midterm.md` (+ zh), Cursor skill `webgpu-midterm`, and `scripts/webgpu-midterm-remaining.{ps1,py}` for the post-default-L2 queue
- Lanes: WG-6 `gpu-canvas-context` shape then L2; S1–S3 guest `rep`/descriptors; named `record-*` maps; optional citable Dawn / WG-5 note
- Point `AGENTS.md`, shape/L2 playbooks, and `guest-shape` / roadmap at the remaining script **Next:** line so agents do not rescan RFC or `cm.rs`
