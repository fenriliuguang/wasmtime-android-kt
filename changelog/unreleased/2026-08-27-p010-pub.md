### Chore — P010 publish 0.1.0 coordinates and CI (2026-08-27)

- Version **`0.1.0`** (drop `-experimental`). groupId `io.github.fenriliuguang.wasmtime.android`
- Publishing workflow: `.github/workflows/publish.yml` (`workflow_dispatch` only). Same GAV to **GitHub Packages** and **Maven Central** (Central Portal via vanniktech; **skip, do not fail**, when secrets or arm64 `libwasmtime_android_kt.so` are missing). Never auto-publish on `main`. Never publish `:smoke-app`
- Consumer coordinates: **`android-webgpu`** (0.x default), `runtime` (`:android` AAR + `.so`, no Dawn), `host-dawn`. `runtime-api` / `runtime-jni` are published **only** as Maven transitives of `runtime` — do **not** depend on them directly
- Tags `v0.x.y` match the coordinate; first Central **press** still needs Portal token + in-memory GPG + rebuilt jniLibs
