### Code — P010 outbound HTTP send (2026-08-27)

- Product `wasi:http/client@0.3.0#send` (0.3 equivalent of outgoing-handler) does a real HTTP/1.1 GET to guest `request` authority (`host:port`) on a helper thread — not in-process 200. `[method]request.set-authority`. **Sandbox:** OS TCP stack + Android INTERNET; no TLS crate / https → `unknown` (no bundled CA). smoke-app `usesCleartextTraffic` for the local instrument. No `wasmtime-wasi`
- Fixture `http_out`; native `wasi_http_handler` outbound test (peer must see `GET /`); device `WasiHttpHandlerInstrumentedTest.outboundSendHitsBoundPeer`
