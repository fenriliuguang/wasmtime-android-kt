### Code — P010 guest-visible cli error-code (2026-08-27)

- Product `wasi:cli` `error-code` includes `unknown` / `io` / `illegal-byte-sequence` / `pipe` (not the full G-err dump). stdout/stderr `write-via-stream` returns `illegal-byte-sequence` when the guest writes a NUL
- Fixture `cli_stdout_err`; native `wasi_cli_stdout` err test; device instrument `WasiCliStdoutInstrumentedTest.writeViaStreamNulIsIllegalByteSequence`. Completes **G-cli-error** on the product stdio path. No `wasmtime-wasi`
