### Code — wasi:webgpu W3 [method]gpu-command-encoder.begin-render-pass (2026-08-15)

- Register WIT `gpu-command-encoder` + `get-encoder` + `[method]gpu-command-encoder.begin-render-pass` (sync; transitional view u32 stub 23; L2 adapter → device → encoder → begin-clear)
- Fixture `fixtures/w1/webgpu_method_begin_render_pass`; native `wasi_webgpu_method_begin_render_pass`; twin instrument `WasiWebGpuMethodBeginRenderPassInstrumentedTest`
- Transitional: not full `gpu-render-pass-descriptor`. Flat `command-encoder-begin-render-pass-clear` kept. Not compliance
