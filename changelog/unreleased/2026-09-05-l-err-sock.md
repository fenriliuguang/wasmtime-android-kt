- L-ERR-SOCK: sockets `error-code` 对齐 WASI 0.3.0 官方 variant（首案 `access-denied`，末案 `other(option<string>)`，去掉 `unknown`）。`create-tcp-socket(ipv6)` → `not-supported`；失败 connect 映射 `connection-refused` / `remote-unreachable` / `other(none)`。
