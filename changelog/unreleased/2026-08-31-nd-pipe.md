### Code — ND-PIPE native bind-group/layout + compute/render pipelines (2026-08-31)

- NativeGpu table-backed `create-bind-group-layout`, `create-pipeline-layout`, `create-bind-group`, `create-compute-pipeline` / `create-render-pipeline` (sync + async) and pipeline-constant records. Dawn C slots remain 0. JNI default unchanged.
- Reuse matching `fixtures/w1` layout / bind-group / pipeline method wasm on NativeGpu via product `define_host`. Leftover table: [`docs/mapping/gap-webgpu-native-dawn.md`](../../docs/mapping/gap-webgpu-native-dawn.md).
