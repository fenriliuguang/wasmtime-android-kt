- L-SOCK-DNS: `wasi:sockets/ip-name-lookup@0.3.0#resolve-addresses`。在 helper 线程做 `ToSocketAddrs`，**不得**在 ART 主线程做 DNS。返回 `list<ip-address>`（本切片 ipv4）；空名 → `invalid-argument`。Android 需要 INTERNET。夹具 `sockets_dns`（`localhost` → `127.0.0.1`）。
