### WebGPU — BIND remaining pin methods to Dawn C (2026-09-02)

- When `libwebgpu_dawn.so` is loaded, remaining pin `[method]`s that Dawn C has a slot call `webgpu.h` (texture / sampler / compute / copies / map / query / bundle / write-texture / work-done / viewport / indexed-indirect / error scopes / adapter features / destroy). Record holes stay Record. Cloud / missing `.so` stays table-backed.
