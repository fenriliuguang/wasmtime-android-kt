- L-HTTP-SVC: `wasi:http/types@0.3.0` remaining incoming-handler shape for a guest `handle`: `[method]request.get-method` / `get-path-with-query` / `get-scheme` / `get-authority` and `[method]response.set-status-code`（官方 `get-status-code` 亦挂）。**不是**监听 HTTP 服务器。产品 linker 仍省略 `[constructor]request` / `[constructor]response`。夹具 `http_svc`。
