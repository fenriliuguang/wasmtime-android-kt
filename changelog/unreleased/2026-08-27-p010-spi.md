### Code — P010-SPI sink ExperimentalHostCallbacks (2026-08-27)

- Move `ExperimentalHostCallbacks` from `runtime-api` (`api` package) to `:runtime-jni` `internal`. `WebGpuBackend` no longer exposes `hostCallbacks()`
- Product attach is `Store.setWebGpuBackend`; Dawn/CPU implement internal `WebGpuBackendHostAttach`. `setHostAdd` / `setExperimentalHost` / `setRequestAdapter` are **not** product API (`@Deprecated`)
- No crate bump; existing Dawn product-path instruments still compile against `GpuBackends`
