### Code — P010 drop WebGPU fixture constructors from the product linker (2026-08-27)

- Product `Linker.create` no longer exports `get-gpu`, `get-device`, `get-gpu-error`, `get-device-lost-info`. `[method]gpu.request-adapter` stays on the product instance
- Instruments / leftover guests use `Linker.createWithFixtureConstructors` (test-only). Native `wasi_webgpu_product_linker` checks product-shaped instantiate fails without `get-gpu` and the test wrap still chains request-adapter
- Existing Dawn present instrument remains the device product-path smoke (explicit attach; test linker for fixture ctors). Cloud has no device
