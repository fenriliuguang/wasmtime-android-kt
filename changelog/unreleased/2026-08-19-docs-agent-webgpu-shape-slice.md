### Docs — agent playbook for wasi:webgpu shape slices (2026-08-19)

- Vendor pinned `wasi:webgpu@0.3.0-rc.2` WIT under `third_party/wasi-webgpu/` so agents do not download upstream
- Add `docs/agent/webgpu-shape-slice.md`, Cursor rule/skill, and `.\scripts\webgpu-shape-remaining.ps1` (WIT `[method]` vs `cm.rs` wraps)
- Hub freeze unchanged; shape-slice DoD is the playbook (narrow tests, copy-one template, no Transitional paragraph rewrites)
