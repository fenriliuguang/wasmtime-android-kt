### Named — cube hitch D21 FrameTimeline / VSyncPredictor (2026-09-01)

- Dawn C 22 s Perfetto on V2458A: 2155/2155 display FrameTimeline frames are `NONE` / `ON_TIME` / `VALID` prediction. `vsync_predictor_recovery` is false. SurfaceView BLAST has no surface-frame timeline, so D21 is not the ~5 s rewind. Not a consume lane.
