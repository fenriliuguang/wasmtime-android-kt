- L-HTTP-TLS: `wasi:http/client@0.3.0#send` 对 `https:host:port` 走 **rustls**（`ring` + `webpki-roots`）。TLS 与 HTTP 写读都在 **helper 线程**，不得在 ART 主线程 / GpuThread。无 `wasmtime-wasi`。本机夹具 `http_https_tls` 打本机 rustls 服务（测试 linker skip-verify 自签证书）。
- `.so` 体积（本 VM `x86_64-unknown-linux-gnu` `cargo build --release` `libwasmtime_android_kt.so`）：**29310576 → 31331352 字节**（+2020776，约 +1.93 MiB）。Android arm64 会因 rustls/ring 有相近增量。
