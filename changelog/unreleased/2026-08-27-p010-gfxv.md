### Feat — P010 Choreographer vsync into gfx on-frame (2026-08-27)

- Host `surface.on-frame` is a 1-slot CM `StreamProducer`: UI Choreographer posts beats (`Store.postGfxVsync`); `poll_produce` writes on the GpuThread CM driver; unconsumed beats drop. `Store.closeGfxOnFrame` / `surfaceDestroyed` closes the stream so guest `run` unblocks. Guest still pulls; no JS `start(callback)`
- Instrument `WasiGfxFrameLoopInstrumentedTest` paces multi-frame present from vsync, not two events at construct. Native `wasi_gfx_frame_loop` simulates GpuThread cadence + drop. Cloud has no device
