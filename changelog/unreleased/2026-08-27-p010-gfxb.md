### Feat — P010 product adapter/device bootstrap for gfx frame loop (2026-08-27)

- Frame-loop guest chains pin `get-gpu` → `[method]gpu.request-adapter` → `[method]gpu-adapter.request-device` (no fixture `get-device`). Instrument `WasiGfxFrameLoopInstrumentedTest` uses product `Linker.create`. Two pre-buffered frames stay until P010-GFXV
- Pin WIT `get-gpu: func() -> gpu` is on the product linker (P010-FIX had grouped it with test-only `get-device`). `get-device` / `get-gpu-error` / `get-device-lost-info` stay fixture-only. No JS callback. No `wasmtime-wasi`
- Native `wasi_gfx_frame_loop`; `wasi_webgpu_product_linker` now expects pin `get-gpu` and still rejects `get-device`. Cloud has no device
