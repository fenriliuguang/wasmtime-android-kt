### Named — cube hitch N4/D2 NativeGpu histograms (2026-09-01)

- NativeGpu logs logcat `GfxHitch` every 120 frames: acquire interval (`<11 / 11–20 / >20ms`) and `vsync→wgpuSurfacePresent` latency (`>8.3ms` phase). Hitch checklists absorb hitch-branch D2/D3 numbers plus the D25 JNI-not-necessary correction. Not a consume lane.
