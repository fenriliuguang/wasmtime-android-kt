### Code — ND-DISP wasi:webgpu NativeGpu | JniBackend dispatch (2026-08-31)

- Pin `wasi:webgpu` imports dispatch `NativeGpu | JniBackend` via `HostState::webgpu_backend`. Default remains JNI (`native_gpu` unset) so existing tests stay green.
- Native slot may be unset until ND-HOST. `jvm::exp_*` leftover unchanged. gfx / `experimental:webgpu-cm` still use the JNI callback directly.
