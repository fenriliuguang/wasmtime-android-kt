### Code — ND-QUEUE native submit / write-buffer / write-texture / work-done (2026-09-01)

- NativeGpu table-backed `queue.submit`, `write-buffer-with-copy`, `write-texture-with-copy`, `on-submitted-work-done`. Guest `list<u8>` is one host copy (no AAR JNI). Dawn C slots remain 0. WIT `async` work-done stays `func_wrap_concurrent` + yield. JNI default unchanged.
- Reuse matching `fixtures/w1` submit / write-buffer / write-texture / work-done method wasm on NativeGpu via product `define_host`. Leftover table: [`docs/mapping/gap-webgpu-native-dawn.md`](../../docs/mapping/gap-webgpu-native-dawn.md).
