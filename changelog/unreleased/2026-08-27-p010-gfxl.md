### Code — P010 product gfx on-frame loop + present (2026-08-27)

- Product `wasi-gfx:surface/surface-webgpu@0.2.0` `context` constructor / `configure` / `get-current-texture` / `present`. Guest async `run` loops `on-frame` → get-current-texture → `queue.submit` → `present` (two pre-buffered `frame-event`s on `GpuThread`; pin `on-frame` is a sync `func`, no stackful CM async). GPU bootstrap still fixture `get-device`. WG-6 one-shot stays. No JS callback. No `wasmtime-wasi`
- Fixture `gfx_frame_loop`; native `wasi_gfx_frame_loop`; device `WasiGfxFrameLoopInstrumentedTest`. Cloud has no device
