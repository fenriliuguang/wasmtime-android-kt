### Code — ND-SURF native canvas `ANativeWindow` surface (2026-09-01)

- NativeGpu stores the Store / `bindCanvasNativeWindow` `ANativeWindow*` (opaque `i64`), creates a table `Surface` on configure when a window is bound, and implements `gpu-canvas-context` configure / get-current-texture / present plus `wasi-gfx` `context.present`. Hitch invariants: Fifo, `setBufferCount(4)` before configure (H9), intern one queue (H25), no acquire-wait of the previous fence (H1), no same-present `close()` (C2), keep-3 + GPU-done retire, submit/`present` idempotent (H8). Dawn C slots remain 0. JNI default unchanged.
- `Store.bindCanvasNativeWindow` forwards the window into `HostState` / NativeGpu. Cube is **not** this lane’s DoD. Named instruments (Cloud has no device): `WasiGfxFrameLoopInstrumentedTest`, `WasiWebGpuMethodCanvasContextPresentInstrumentedTest`, `WasiWebGpuDawnGuestCanvasPresentInstrumentedTest`, `WasiWebGpuCanvasContextFrameLifetimeInstrumentedTest`. Leftover table: [`docs/mapping/gap-webgpu-native-dawn.md`](../../docs/mapping/gap-webgpu-native-dawn.md).
