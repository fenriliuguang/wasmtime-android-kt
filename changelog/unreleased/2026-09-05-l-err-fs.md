- L-ERR-FS: `wasi:filesystem` `error-code` 对齐 WASI 0.3.0 官方 variant（首案 `access`，末案 `other(option<string>)`，去掉 `unknown`）。`open-at("..")` 仍为 `access`；缺失 descriptor → `bad-descriptor`；r/w IO 映射 `io` / `is-directory` 等。
