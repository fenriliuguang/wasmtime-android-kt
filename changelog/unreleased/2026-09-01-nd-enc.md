### Code — ND-ENC native command-encoder / passes / draws / copies (2026-09-01)

- NativeGpu table-backed `create-command-encoder`, `begin-render-pass` / `begin-compute-pass`, pass draws / dispatch, copies, debug, `finish`, and query-sets. Dawn C slots remain 0. JNI default unchanged.
- Reuse matching `fixtures/w1` encoder / begin-render-pass / render-pass-end / finish (plus compute-pass, draw, copy, query-set) method wasm on NativeGpu via product `define_host`. Leftover table: [`docs/mapping/gap-webgpu-native-dawn.md`](../../docs/mapping/gap-webgpu-native-dawn.md).
