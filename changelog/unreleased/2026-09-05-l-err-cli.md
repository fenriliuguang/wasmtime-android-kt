- L-ERR-CLI: `wasi:cli` `error-code` 对齐 WASI 0.3.0 官方 enum（`io` / `illegal-byte-sequence` / `pipe`，去掉 `unknown`）。stdout/stderr 写路径：oneshot 取消 → `pipe`；含 NUL → `illegal-byte-sequence`；非法 UTF-8（无 NUL）→ `io`。
- 夹具：`cli_*.wat` 的 `error-code` 改为三值 enum；新增 `cli_stdout_io.wat`（写 `0x80` 期望 `io`）。
