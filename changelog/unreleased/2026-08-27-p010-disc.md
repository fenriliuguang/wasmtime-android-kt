### Code — P010 dual-track Store discover factory (2026-08-27)

- Product default stays `Store.create(engine)` (`discoverWebGpuBackend = false`). Default-bundle convenience is `Store.createWithDiscoveredBackend` (ServiceLoader; prefer `id == "dawn"`)
- Explicit `setWebGpuBackend` always wins: later `discoverWebGpuBackend()` is a no-op when a backend is already attached. Zero factories leave `request-adapter` **`none`**
- Keep published R8 `consumer-rules.pro` on `:host-dawn` / `:android-webgpu` (`WebGpuBackendFactory` + Dawn factory). Existing Dawn present instrument still uses explicit attach
