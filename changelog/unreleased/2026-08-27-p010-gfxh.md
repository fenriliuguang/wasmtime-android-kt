### Code — P010 host wasi-gfx surface on-frame stream (2026-08-27)

- Product `wasi-gfx:surface@0.2.0` constructor + `[method]surface.on-frame` returns a CM `stream<frame-event>`. Guest pulls; no JS-style callback. Vsync payload is produced on a helper thread named `GpuThread`. Pin `on-frame` is a sync `func` (this slice does not enable Wasmtime stackful CM async). No `surface-webgpu` / present loop (P010-GFXL). No `wasmtime-wasi`
- Fixture `gfx_on_frame`; native `wasi_gfx_surface` (guest `run` returns 1; event thread name is `GpuThread`). Device not required this lane. Cloud has no device
