### Code — P010 outbound TCP (2026-08-27)

- Product `tcp-socket.connect` **dials** guest non-loopback `ip-socket-address` IPv4:port (not ignore-port + echo pair). Loopback still uses the W7/SK2 echo pair. **Sandbox:** outbound with INTERNET; no listen / UDP by default
- Fixture `sockets_tcp_out`; native `wasi_sockets_tcp` outbound test (echo server must see `P3SK`); device `WasiSocketsTcpInstrumentedTest.outboundNonLoopbackEchoHitsBoundPeer`. No `wasmtime-wasi`
